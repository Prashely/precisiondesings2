{
  "name": "precision-designs-agency",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "echo 'Building static site' && mkdir -p dist && cp *.html dist/ && cp -r images dist/",
    "dev": "npx serve . -p 3000",
    "start": "npx serve . -p 3000"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
